# AI 工程协作 Token 节省规则

> 适用对象：ChatGPT、Codex、OpenCode，以及后续参与项目的任何 AI 协作者  
> 文档性质：长期工程协作规则  
> 核心目标：减少无效 Token 消耗，同时保证必要上下文、关键代码和真实工程事实不被省略

---

## 一、核心原则

Token 节省不等于少看，也不等于跳过。

正确做法是：

> 只读取当前任务真正需要的内容，并在发现依赖时继续补读。

禁止为了节省 Token：

- 不看相关源码
- 不看当前状态
- 不看当前任务
- 不验证实际实现
- 凭经验猜测文件结构
- 根据日志标题猜测代码行为
- 跳过关键依赖文件
- 只修改提示词，不检查本地处理逻辑
- 在信息不足时继续施工

Token 节省的目标不是压缩理解，而是减少无关信息。

---

## 二、标准读取顺序

每次开始任务时，按以下顺序读取：

1. 长期工程规则
2. 当前项目状态
3. 当前任务说明
4. 当前任务直接相关的源码
5. 当前源码引用的必要依赖
6. 上一份已验收阶段日志的摘要

默认不读取：

- 全部历史日志
- 全部工程源码
- 与当前任务无关的页面
- 已废弃实验脚本
- 已确认且没有变化的旧方案
- 与当前功能无关的领域代码
- 大量完整终端输出
- 重复出现的背景说明

---

## 三、按需展开规则

采用“逐层展开”，不采用“一次性通读”。

### 第一层：任务定位

先搜索当前任务相关关键词、类名、函数名、文件名和数据结构。

例如账本任务可优先搜索：

```text
money
ledger
expense
income
pending_payment
balance
storage
parser
normalize
schema
preview
```

### 第二层：读取直接相关文件

只读取搜索结果中明确参与当前功能的文件。

### 第三层：追踪必要依赖

只有在当前文件确实调用其他模块时，才继续读取对应依赖。

### 第四层：遇到信息缺口时补读

如果无法确认数据流、调用链、字段来源或错误位置，必须继续读取实际代码，不允许用猜测补齐。

---

## 四、禁止盲目全面读取

不得因为“想完整了解项目”而默认读取整个仓库。

只有以下情况允许扩大读取范围：

- 当前问题跨越多个模块
- 调用链无法从局部代码确认
- 数据结构在多个文件中定义
- 当前修改可能影响公共基础层
- 用户明确要求完整工程审查

扩大范围前，应先说明：

1. 为什么当前范围不足
2. 需要补读哪些文件
3. 每个文件与任务的关系
4. 预计增加的工作量

---

## 五、禁止在信息不足时猜测

当手中没有足够代码或真实运行结果时，不允许：

- 猜测问题位于某个函数
- 猜测某字段在某层丢失
- 猜测某设置已经生效
- 猜测某文件存在
- 猜测某 API 行为
- 猜测某错误由模型、GUI 或存储层引起
- 根据一次截图直接下工程结论

正确做法是：

1. 先拿到相关代码
2. 检查真实调用链
3. 查看实际日志或运行结果
4. 再提出结论和修改方案

---

## 六、失败方案停止规则

同一方案连续失败两次后，禁止原样继续尝试。

必须停下来检查：

- 原始假设是否错误
- 是否读错代码层
- 是否遗漏依赖
- 是否把表现层问题误判为数据层问题
- 是否把模型问题误判为连接问题
- 是否只改提示词而未检查本地逻辑
- 是否同时改变了多个变量
- 是否缺少真实验收

连续两到三轮仍卡在同一位置时，必须重新定义问题，不得继续堆补丁。

---

## 七、单变量修改规则

排查问题时，一次只改变一个主要变量。

禁止同时：

- 改 Prompt
- 改 Schema
- 改 normalize
- 改 GUI
- 改存储结构
- 改模型参数

否则无法判断是哪项改动产生结果。

标准方式：

1. 明确问题表现
2. 确认问题所在层
3. 只修改对应层
4. 运行针对性测试
5. 通过后再进入下一层

---

## 八、文档分层规则

不同信息必须分开保存，避免每次重复输入。

### 长期规则

保存长期有效的工程原则、边界和协作方式。

### 当前状态

保存项目目前真实完成到哪里、哪些功能稳定、哪些问题仍存在。

### 当前任务

只保存本轮目标、允许修改范围、实施步骤和验收标准。

### 阶段日志

只记录已经通过验收的阶段结果。

### 历史实验

失败实验、临时脚本和一次性排查结果不进入正式阶段日志。

---

## 九、日志节省规则

正式日志应记录结论，不应复制过程噪音。

应记录：

- 上一阶段总结
- 本轮实际操作
- 本轮完成内容
- 发现的流程性问题
- 偏差原因
- 修正方向
- 新增效率禁区
- 待解决事项
- 当前状态
- 下一步规划

不应记录：

- 完整终端输出
- 大段重复代码
- 全部失败尝试细节
- 已删除的临时脚本内容
- 与当前阶段无关的背景
- 已在长期规则中存在的完整条款

必要时只保留关键证据，例如错误码、核心日志行和最终结论。

---

## 十、历史信息使用规则

默认只读取：

- 最近一份已验收日志摘要
- 当前状态
- 当前任务

只有在以下情况才向前追溯：

- 当前问题曾经出现过
- 需要确认某项历史决策
- 怀疑旧阶段引入回归
- 当前状态与实际代码不一致
- 用户明确要求回顾历史

禁止每次施工都加载全部历史 Markdown。

---

## 十一、源码读取规则

读取源码时，优先顺序为：

1. 当前任务入口文件
2. 数据结构定义
3. 业务处理函数
4. 存储层
5. GUI 或调用层
6. 测试文件
7. 必要配置

不要先读无关 UI、无关 domain 或整个项目目录。

如果问题涉及完整数据链，应按链路检查：

```text
输入
→ Parser
→ Normalize
→ Deduplicate
→ Schema
→ Storage
→ GUI Preview
```

不得只检查其中一层就下结论。

---

## 十二、测试节省规则

优先运行与当前改动直接相关的测试。

顺序建议：

1. 单元测试
2. 当前模块测试
3. 真实输入验收
4. 必要时再运行全量测试

禁止在小修复后默认运行：

- 大型性能基准
- 多模型对比
- 全量环境扫描
- GPU 深度监控
- 与当前目标无关的回归实验

扩大测试范围前，必须说明原因。

---

## 十三、工具与脚本规则

临时脚本必须满足：

- 用途明确
- 文件名清楚
- 不写入正式数据
- 不修改正式配置
- 使用后可清理
- 不在根目录长期堆积

禁止为了回答一个简单问题，创建大量重复脚本。

如果已有脚本可以完成任务，优先复用，不要重复造轮子。

---

## 十四、任务范围控制

每轮只处理一个明确项目或一个明确子问题。

禁止把以下内容混在同一轮：

- 功能开发
- 性能优化
- 模型研究
- UI 重构
- 数据迁移
- 文档体系重做

如发现其他问题：

1. 记录
2. 放入待解决事项
3. 不自动扩展当前任务

---

## 十五、输出节省规则

施工端汇报应简洁但完整。

必须包含：

- 做了什么
- 修改了哪些文件
- 是否修改范围外内容
- 测试结果
- 真实验收结果
- 尚未完成内容
- 风险
- 下一步建议

不需要：

- 重复用户原始需求
- 重复完整规则
- 逐行解释所有代码
- 输出大量无关日志
- 用长篇文字包装简单结论

---

## 十六、效率禁区

以下行为视为效率禁区：

1. 未读取相关源码就猜问题位置。
2. 同一方案失败两次后继续原样尝试。
3. 为节省 Token 跳过必要代码。
4. 为追求全面默认读取整个仓库。
5. 只看日志，不看实际实现。
6. 只改 Prompt，不检查 normalize、Schema 和 storage。
7. 同时修改多个变量，导致无法定位结果来源。
8. 把失败实验写入正式完成日志。
9. 未通过用户验收就更新当前完成状态。
10. 已有明确证据后仍继续重复测试。
11. 简单问题升级为大型基准或架构研究。
12. 未经批准自动扩大任务范围。
13. 在代码不足时用经验补全工程事实。
14. 重复创建功能相似的临时脚本。
15. 每轮都加载全部历史文档。
16. 用聊天记录代替项目文件。
17. 因为担心遗漏而复制大量无关上下文。
18. 因为追求速度而省略关键验证。
19. 没有确认问题层级就直接动代码。
20. 没有真实运行结果就宣布完成。

---

## 十七、遇到不确定情况时的处理方式

当信息不足时，必须明确说：

```text
当前信息不足以确认问题位置。
需要补读以下文件或获得以下运行结果后再判断。
```

然后列出最小必要材料。

禁止用模糊措辞伪装确定性。

---

## 十八、阶段完成后的信息压缩

每个阶段验收通过后，应把讨论压缩为：

- 已完成事实
- 稳定基线
- 遗留问题
- 新增效率规则
- 下一步任务

后续施工只读取压缩后的阶段状态，不再重复加载整段对话。

---

## 十九、最终判断标准

Token 是否被有效节省，不看输入有多短，而看：

- 是否减少了无关阅读
- 是否避免了重复试错
- 是否准确定位问题
- 是否减少返工
- 是否保留必要上下文
- 是否能用更少轮次完成验收
- 是否没有因为省略信息造成新错误

真正的节省不是少读几千字，而是少走几轮弯路。

---

## 二十、执行口令

所有 AI 施工端开始任务前，默认遵守：

```text
先定位，再读取。
只读相关，不读全部。
信息不足，补读代码。
连续失败，停止猜测。
一次只改一个主要变量。
未验收，不写完成日志。
不为节省 Token 牺牲正确性。
```
