# AI 工程项目执行循环流程

> 适用对象：ChatGPT、Codex、OpenCode，以及后续参与项目施工的任何 AI 协作者  
> 文档性质：每个独立项目或子项目的标准执行流程  
> 核心目标：把大任务拆成可检查、可验收、可回滚的小阶段，避免越界修改、重复返工和上下文混乱

---

## 一、适用范围

本流程适用于：

- 新功能开发
- 现有功能修复
- 数据结构调整
- GUI 功能施工
- 解析逻辑调整
- 存储层改动
- 性能优化
- 独立技术债修复

一个“项目”必须满足：

- 目标单一
- 验收结果明确
- 修改范围可控
- 可以独立运行
- 可以独立测试
- 可以独立停止
- 可以独立回滚

如果一个任务同时包含多个互不依赖的目标，应先拆成多个项目，不能一次总承包。

---

## 二、项目开始前必须读取的文件

每个项目开始前，施工端按顺序读取：

1. `AI工程协作_Token节省规则.md`
2. `AI工程项目执行循环流程.md`
3. `AI工程阶段日志格式.md`
4. 当前项目状态文件
5. 当前项目任务说明
6. 当前任务直接相关的源码
7. 必要依赖文件

默认不读取：

- 全部历史日志
- 全部项目源码
- 与当前项目无关的功能
- 已废弃实验脚本
- 无关终端输出
- 未经筛选的旧讨论记录

---

## 三、项目执行总流程

每个项目严格执行以下九个阶段：

```text
1. 任务定义
2. 施工前调查
3. 方案输出
4. 用户确认
5. 正式施工
6. 施工端自检
7. 用户验收
8. 阶段冻结
9. 日志生成
```

未完成当前阶段，不得自动进入下一阶段。

---

# 第一阶段：任务定义

## 1.1 明确项目名称

项目名称必须具体，例如：

```text
账本核心 MVP
预算管理 MVP
账本分类统计
月度图表
月报生成
```

不得使用过于宽泛的名称，例如：

```text
完善财务功能
优化整个系统
把账本做完整
```

## 1.2 明确本轮唯一目标

必须用一句话说明：

> 本轮完成什么，完成后用户能够实际做什么。

例如：

> 完成账本核心，使用户可以记录已发生支出、已收到收入、待支付事项，并正确计算当前余额。

## 1.3 明确本轮不做什么

每个项目都必须列出暂不处理的内容。

例如账本核心阶段不做：

- 预算
- 图表
- 分类统计
- 月报
- 银行同步
- 自动提醒

这些内容进入后续路线，不得顺手扩展。

---

# 第二阶段：施工前调查

## 2.1 搜索相关代码

施工端先搜索当前项目涉及的关键词、类名、函数名、数据结构和测试。

例如账本核心可搜索：

```text
money
ledger
expense
income
pending_payment
balance
storage
schema
parser
normalize
GUI
```

## 2.2 检查真实实现

施工前必须确认：

- 当前相关文件在哪里
- 当前已有数据结构是什么
- 当前调用链是什么
- 当前存储方式是什么
- 当前 GUI 如何展示
- 当前测试覆盖什么
- 是否已经存在可复用逻辑
- 是否存在重复结构

## 2.3 禁止猜测

在未读取相关源码前，不允许：

- 猜文件位置
- 猜问题层级
- 猜字段流向
- 猜 API 行为
- 猜当前 Schema
- 猜存储结构
- 猜某功能尚未存在

如果信息不足，必须先补读最小必要代码。

---

# 第三阶段：方案输出

施工端在改代码前，必须先输出完整施工方案。

方案至少包括：

1. 当前理解
2. 当前真实代码结构
3. 本轮目标
4. 预计修改文件
5. 每个文件为什么需要修改
6. 明确不修改的文件
7. 数据结构变化
8. 数据流变化
9. GUI 改动点
10. 测试计划
11. 风险点
12. 验收标准
13. 施工顺序
14. 停止条件

此阶段不得开始修改代码。

---

# 第四阶段：用户确认

用户或 ChatGPT 审查施工方案。

重点检查：

- 是否理解错需求
- 是否扩大范围
- 是否漏掉关键数据链
- 是否误改现有稳定功能
- 是否创建重复结构
- 是否一次做太多
- 验收标准是否可实际检查
- 是否存在不必要的重构

未确认前，施工端不得开工。

如果方案需要修改，应先修正方案，再进入施工。

---

# 第五阶段：正式施工

## 5.1 严格按方案修改

施工端只允许修改已批准文件。

如果施工过程中发现必须修改范围外文件，必须：

1. 立即停止
2. 说明原因
3. 列出拟修改文件
4. 说明每个文件为何必须修改
5. 等待批准

不得先改后报。

## 5.2 一次只处理一个主要问题

不允许在同一轮同时进行：

- 功能开发
- 大规模重构
- 模型研究
- 性能基准
- 最终 UI 美化
- 无关技术债修复

发现其他问题时，记录到“待解决事项”，不自动扩展。

## 5.3 不修改正式数据

开发和测试期间：

- 使用测试数据
- 使用临时目录
- 不污染正式账本
- 不写入正式状态
- 不删除用户文件
- 不覆盖真实日志

---

# 第六阶段：施工端自检

施工完成后，施工端必须自行检查。

## 6.1 代码检查

至少检查：

- 程序能否启动
- 是否存在语法错误
- 是否存在运行错误
- 是否存在控制台报错
- 是否修改范围外文件
- 是否破坏现有功能
- 是否生成无关临时文件
- 是否污染正式数据

## 6.2 测试检查

优先顺序：

1. 当前函数单元测试
2. 当前模块测试
3. 当前项目真实输入测试
4. 必要时全量测试

不得因为一个小改动自动运行大型性能基准。

## 6.3 输出施工报告

施工端完成后必须汇报：

1. 实际修改文件
2. 每个文件改了什么
3. 是否修改范围外文件
4. 新增测试
5. 测试结果
6. 当前已知问题
7. 未完成内容
8. 用户需要验收什么
9. 当前是否可以进入验收

施工端不得自行宣布“正式完成”。

---

# 第七阶段：用户验收

用户负责真实验收。

## 7.1 验收必须使用真实操作

例如账本功能应实际检查：

- 输入一句支出
- 输入一句收入
- 输入一句待支付
- 确认写入
- 查看列表
- 检查余额
- 重启程序
- 删除错误记录

不能只看自动测试通过。

## 7.2 验收结果分为三种

### 通过

功能达到验收标准，可以冻结阶段。

### 有小问题

继续留在当前项目修复，不进入下一项目。

### 不通过

重新检查方案、实现和偏差原因。

## 7.3 未通过时禁止

- 写正式阶段日志
- 更新当前状态为完成
- 自动开始下一项目
- 建立稳定基线
- 把实验版本视为正式成果

---

# 第八阶段：阶段冻结

只有用户验收通过后，才进行阶段冻结。

冻结内容包括：

- 将当前项目标记为已完成
- 更新当前状态
- 记录稳定版本
- 如有 Git，创建独立 commit
- 记录遗留问题
- 确认下一项目入口

冻结后，当前功能视为稳定基线。

后续项目不得随意推翻。

如必须修改稳定基线，必须单独建立新项目。

---

# 第九阶段：日志生成

验收通过后，生成正式阶段日志。

日志必须使用：

```text
AI工程阶段日志格式.md
```

正式日志只记录：

- 上一阶段总结
- 本轮操作
- 本轮完成
- 本轮发现的问题
- 偏差原因
- 修正方向
- 新增效率禁区
- 待解决事项
- 当前状态
- 下一步规划

失败实验、临时排查和未验收结果不写入正式日志。

---

## 四、停止条件

出现以下任一情况，施工端必须停止：

1. 发现任务理解可能错误
2. 需要修改范围外文件
3. 当前代码与任务描述明显不一致
4. 同一方案连续失败两次
5. 无法确认问题所在层
6. 需要改变核心 Schema
7. 需要修改正式数据
8. 发现可能破坏稳定功能
9. 用户要求先汇报
10. 已完成当前项目目标

停止后必须先汇报，不得继续自由探索。

---

## 五、项目完成标准

一个项目只有同时满足以下条件，才算彻底完成：

- 功能已实现
- 自动测试通过
- 真实操作通过
- 用户明确验收通过
- 没有范围外修改
- 没有污染正式数据
- 已知问题已记录
- 当前状态已更新
- 正式阶段日志已生成
- 下一项目尚未自动开始

---

## 六、每个项目的简明执行清单

### 开工前

- [ ] 已读取三个长期规则 Markdown
- [ ] 已读取当前状态
- [ ] 已读取当前任务
- [ ] 已搜索相关代码
- [ ] 已确认真实调用链
- [ ] 已输出施工方案
- [ ] 用户已确认方案

### 施工中

- [ ] 只修改批准文件
- [ ] 未扩大任务范围
- [ ] 未污染正式数据
- [ ] 未同时修改多个无关系统
- [ ] 发现范围外问题已停止汇报

### 施工后

- [ ] 已运行相关测试
- [ ] 已完成真实输入自检
- [ ] 已输出修改清单
- [ ] 已说明风险和未完成内容
- [ ] 已等待用户验收

### 验收后

- [ ] 用户明确通过
- [ ] 当前状态已更新
- [ ] 稳定版本已冻结
- [ ] 已生成正式阶段日志
- [ ] 下一项目另行创建任务

---

## 七、执行口令

```text
一个项目，一次施工。
先读规则，再看代码。
先给方案，不先开工。
范围外问题，先停后报。
自动测试不能代替真实验收。
未经验收，不写正式日志。
当前项目完成后立即停止。
```
